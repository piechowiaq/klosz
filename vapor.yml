id: 50694
name: Klosz
environments:
  production:
    memory: 1024
    cli-memory: 512v
    runtime: 'php-8.2:al2'
    build:
      - 'composer install --no-dev'
      - 'php artisan event:cache'
      - 'npm run production'
      # - 'npm ci && npm run build && rm -rf node_modules'
